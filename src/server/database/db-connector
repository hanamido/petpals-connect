const mysql = require('mysql');

// Create 'connection pool' using the provided credentials
var pool = mysql.createPool({
    connectionLimit: 10, 
    host: "localhost",
    user: "yourusername",
    password: "yourpassword",
    database: "petpals_db"
});

// Function to get all pets from database

// connection.connect(function (err) {
//     if (err) throw err;
//     connection.query("SELECT * FROM Pets", function (err, result, fields) {
//         if (err) throw err;
//         console.log(result);
//     });
// })

// Export for use in our app
module.exports.pool = pool;